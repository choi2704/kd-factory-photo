# 강동자바라 제작관리 - 작업완료 사진 버전

## 동작
1. 사무실에서 주문 등록
2. 공장 작업자가 휴대폰으로 factory.html 접속
3. 해당 주문에서 작업완료 사진 촬영
4. `작업완료` 버튼 클릭
5. 사진이 Supabase Storage에 업로드됨
6. 주문 상태가 작업완료로 변경됨
7. 사무실 화면에 실시간으로 작업완료 + 완료사진 표시

## 처음 한 번 설정
1. Supabase 프로젝트 생성
2. SQL Editor에서 `setup.sql` 실행
3. Storage에서 `completion-photos` 이름의 Public bucket 생성
4. `config.js`에 Project URL / anon key 입력
5. 전체 파일을 GitHub Pages에 업로드

## 주의
현재는 빠른 사내용 프로토타입이라 anon 접근 정책을 사용합니다.
외부 공개 주소로 오래 운영할 경우 로그인/권한 기능을 추가하는 것이 안전합니다.


## 2026-08-11 연결 오류 수정
- GitHub Pages/브라우저가 예전 config.js를 읽지 않도록 캐시 무효화 적용
- Supabase URL 공백/끝 슬래시 자동 정리
- `test.html` 연결 테스트 페이지 추가

업로드 후:
1. 저장소의 기존 파일을 이 버전으로 덮어쓰기
2. 1~2분 후 `https://choi2704.github.io/kd-factory-photo/test.html` 접속
3. `연결 테스트` 클릭
4. 성공이면 office.html / factory.html 사용


## 주문 수정 기능 추가
사무실 주문 현황의 각 주문에 `수정` 버튼이 추가되었습니다.

사용 방법:
1. 수정할 주문의 `수정` 버튼 클릭
2. 왼쪽 입력칸에 기존 내용 자동 불러오기
3. 필요한 내용 변경
4. `수정 저장` 클릭
5. Supabase가 업데이트되고 공장 휴대폰 화면에도 자동 반영

Supabase SQL이나 테이블을 새로 만들 필요는 없습니다.


## 공장 작업완료 취소 기능
공장용 완료 탭에서 `작업완료 취소 → 작업중으로` 버튼을 누르면
실수로 완료 처리한 주문을 다시 작업중 상태로 되돌릴 수 있습니다.

- 완료 사진은 기록 보존을 위해 그대로 남겨둡니다.
- 상태와 완료시간만 작업중으로 되돌립니다.
- 사무실 화면에도 실시간 반영됩니다.


## 출고관리 시스템 추가
새 주문 등록 시:
- 출고일 달력 선택
- 배송내용 입력

사무실 하단 `출고관리`에서:
- 오늘 / 전날 / 다음날 / 날짜 직접 선택
- 선택한 날짜의 출고 주문만 확인
- 고객명 / 주문내역 / 주소 / 배송내용 / 작업상태 확인
- 수정 버튼으로 출고일과 배송내용도 수정

### 최초 1회
Supabase SQL Editor에서 `shipping_migration.sql` 내용을 실행해야 합니다.
기존 주문 및 사진 데이터는 그대로 유지됩니다.


## 2026-08-12 UI 변경
- 출고관리 영역을 사무실 화면 상단으로 이동
- 제목을 `출고관리`로 간단하게 변경

## 3열 주문 + 출고 달력
- 사무실 주문현황을 PC에서 한 줄에 3개 카드로 표시
- 출고관리를 월 달력 형태로 변경
- 출고일에는 상호명만 표시
- 상호명을 누르면 새창으로 주문/배송/주소/메모/완료사진 상세 확인

## 2026-08-12 긴급 수정
- 누락되었던 출고관리 달력 영역 복구
- 달력 버튼 요소를 명시적으로 연결해 페이지 로딩 중 JS 중단 문제 수정
- 기존 Supabase 연결정보(config.js) 그대로 유지
- 기존 주문 데이터는 Supabase에 그대로 유지되며 다시 표시됩니다.

## 출고관리 별도 페이지
- 제작관리 메인(office.html)에서는 새 주문 등록 + 주문현황 3열만 표시
- 상단 `출고관리` 버튼을 누르면 shipping.html로 이동
- shipping.html에서 월 달력 확인
- 출고일에는 상호명만 표시
- 상호 클릭 시 새창 상세보기

## 주문현황 3열 수정
- 실제 주문목록 요소(`#list`)에 3열 그리드를 적용
- PC에서는 한 줄에 3건
- 화면이 좁아지면 2건, 모바일은 1건
- 기존 출고관리 별도 페이지 기능 유지


## 하남 사무실 이동 표시
- 배송내용 아래 `하남 사무실 이동` 체크박스 추가
- 체크된 주문은 출고 달력에서 분홍색
- 일반 당일 출고 주문은 기존 파란색
- 상호명 클릭 상세창에도 출고 구분 표시

### 최초 1회
Supabase SQL Editor에서 `hanam_migration.sql` 내용을 실행해야 합니다.

## 출고달력 최종 수정
- 일반 출고: 파란색
- 하남 사무실 이동: 분홍색
- 공장에서 작업완료 상태(done)가 되면 상호명 뒤에 `(완)` 자동 표시
- 상호 클릭 상세 팝업 유지


## 화물 배차관리 v1
- 제작관리 상단에 `🚚 배차관리` 버튼 추가
- 출고일이 입력된 주문을 자동으로 배차관리 화면에 표시
- 큰 카드형 UI로 배차대기 / 배차완료 / 배송완료 구분
- 물품인수증 사진, 차량번호, 기사명/연락처, 운임, 메모 저장
- 여러 직원이 동시에 사용하면 Supabase 실시간 갱신
- 출고달력에는 배차완료 시 `🚚`, 배송완료 시 `(배송완)` 표시
- 기존 하남 분홍색 / 일반 출고 파란색 / 작업완료 `(완)` 유지

### 최초 1회
Supabase SQL Editor에서 `dispatch_migration.sql` 내용을 실행하세요.
